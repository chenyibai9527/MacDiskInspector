# 安全政策

## 支持范围

安全修复优先发布在最新版本。公开发行后，至少维护当前版本和前一个次版本。

## 报告漏洞

请优先使用代码托管平台的 Private Vulnerability Reporting / Security Advisory 私密报告，不要先创建公开 Issue。

报告应包含：

- 受影响版本和 macOS 版本；
- 可复现步骤与最小测试目录；
- 是否可能越过用户选择目录、跟随链接、执行命令或修改文件；
- 日志中是否包含真实用户名、路径或文件名。

报告前请先移除私人路径、聊天数据库和文档内容。项目维护者确认漏洞并准备修复前，请不要公开利用细节。

## 安全不变量

- 无自动删除、移动或修改；
- 无 `sudo`、privileged helper 或任意 Shell；
- 无远程规则字符串；
- 无路径、文件名和统计数据上传；
- 用户选择目录只读沙盒权限；
- 符号链接不跟随，默认不跨卷；
- 权限失败作为覆盖缺口显示。
